class Flower
    {
        color: number;
        x: number;
        y: number;

        public constructor(color: number, x: number, y: number)
        {
            this.color = color;
            this.x = x;
            this.y = y;
        }
    }